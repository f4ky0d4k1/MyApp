<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.sfedu.schedule">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="ru.sfedu.schedule.network.App"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApp3">
        <activity android:name="ru.sfedu.schedule.activities.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ru.sfedu.schedule.activities.requestActivity.RequestActivity"
            android:label="@string/activity_request_title">
        </activity>
        <activity android:name="ru.sfedu.schedule.activities.scheduleActivity.ScheduleActivity"
            android:label="@string/activity_schedule_title">
        </activity>
        <activity android:name="ru.sfedu.schedule.activities.RegistrationActivity"
            android:label="@string/activity_registration_title">
        </activity>
        <activity android:name="ru.sfedu.schedule.activities.ProfileActivity"
            android:label="@string/activity_personal_information_title">
        </activity>
        <activity android:name=".activities.AdminPanelActivity"
            android:label="@string/activity_admin_panel_title">
        </activity>
    </application>

</manifest>